rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /tents/{tentId} {
      allow read: if true;
      allow write: if false;
    }

    match /tents/{tentId}/{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
